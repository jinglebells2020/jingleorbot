# Copy this to /etc/ticalc.env on the Pi and fill in the value.
# The systemd unit (ticalc-bridge.service) reads from /etc/ticalc.env via
# EnvironmentFile=. The bridge itself reads ANTHROPIC_API_KEY from os.environ.

ANTHROPIC_API_KEY=sk-ant-api03-...
